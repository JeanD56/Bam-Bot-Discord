const Discord = require("discord.js");
const config = require('../../config.json');
const color = require('../../colors.json');

module.exports = {
    config: {
        name: "Spam",
        command: "spam",
        alias: [],
        category: "Owner",
        usage: "[off; on]",
        description: "donne acces a ce bot pour discuter",
        autorisation: ""
    },
    run: async (bot, message, args) => {
        
        
    }
}